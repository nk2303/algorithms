//Breaking array into smaller arrays, sort the arrays together.
//I dont' really see how this is faster yet.
//Best and worst case: O(nlogn) always cost same time.

//write a helpder funtionc

const merge = () => {}
const mergeSort = (arr) => {
    for(let i = 1; i < arr.length; i++){
        console.log(i)
    }
    return arr
}