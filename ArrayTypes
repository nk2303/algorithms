Javscript syntax:
- Converting string to number:
    var a = parseInt("string")

- Try to see what else can happen inside a for loop.

- % for calculating
    10 % 3 = 1

- Sorting: a after b if return positive
    MODIFY ARRAY: numberArr.sort((a, b) => a - b);
    UMODIFY: [...numberArr].sort((a, b) => a - b);

- Reverse an array
    modify original: array.reverse();
    
- All kind of sorting that needs to save new variable
    let previous = nums[nums.length -1]
    for (let i = 0; i < nums.length; i++) {
        let current = nums[i];
        nums[i] = previous;
        previous = current;
    }

- Slice returns copy
    slice(start,end)
    [1,2,3,4].slice(0,3)

- Splice modifies original
    arguments (index, number of delete elements before index, insert element)
    ['Jan', 'March', 'April', 'June'].splice(1, 0, 'Feb');

- Deconstruct syntax

- Merge two aaray:
    array1.concat(array2)

- Find min, max:
    Math.min(...array)
    Math.max(...array)

- Reduce:
    arr.reduce(( accumulator, currentValue ) => accumulator + currentValue, optionalInitialValue)