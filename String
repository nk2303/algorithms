- toLowerCase
    string.toLowerCase()

- toUpperCase
    string.toUpperCase()

- check substring :
    string.substring(substring) // return the substring
    string.includes(substring) // true/false

- creat alphabet :
    function genCharArray(charA, charZ) {
        var a = [], i = charA.charCodeAt(0), j = charZ.charCodeAt(0);
        for (; i <= j; ++i) {
            a.push(String.fromCharCode(i).toUpperCase());
        }
        return a;
    }

- substring :
    string.substring(beginIndex, endIndex) // return substrin

- string.split(" ")
