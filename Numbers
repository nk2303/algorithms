- With number-only input, first remember
- Becareful of 0. It's almost always dge cases.

- Prime number : 
    1.can only divisible by 1 and itself and greater than 1.
    2.the number n must not be divisible by any number > n / 2

- % for calculating
    10 % 3 = 1

- Minimum number
    Number.NEGATIVE_INFINITY

- Converting string to number:
    var a = parseInt("string")